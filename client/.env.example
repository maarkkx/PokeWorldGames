# Solo necesario si el frontend se despliega en otro dominio que el backend.
# Sin barra final. Ejemplo: https://api.pokeworldgames.example
# VITE_API_URL=https://api.pokeworldgames.example

# Desarrollo local: usa /api (proxy de Vite). Ver .env.development.
